
export const overlays = {
  authentication: 'authentication',
  dashboard: 'dashboard',
  search: 'search',
  camera: 'camera',
  notifications: 'notifications',
  profile: 'profile',
}

export const apis = {
  
}
